{
  "name": "bass-analyzer-tests",
  "private": true,
  "description": "Verification harness for index.html",
  "scripts": {
    "test": "node verify.js index.html && node dsptest.js index.html && node smoke.js index.html",
    "static": "node verify.js index.html",
    "dsp": "node dsptest.js index.html",
    "smoke": "node smoke.js index.html",
    "serve": "python3 -m http.server 8080"
  },
  "devDependencies": { "jsdom": "^24.0.0" }
}
